interface:
  display_name: "Backquant"
  short_description: "Repo-specific patterns and workflows for backquant"
  default_prompt: "Use the backquant repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true